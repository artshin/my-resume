<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Resume - {{profile.name}}">
  <title>{{profile.name}} - Resume</title>
  <style>
    {{{styles}}}
  </style>
</head>
<body>
  <main class="resume{{#if options.twoPage}} resume--two-page{{/if}}{{#if options.compact}} resume--compact{{/if}}">

    {{!-- Header with contact info --}}
    {{> header}}

    {{!-- Professional summary --}}
    {{> summary}}

    {{!-- Technical skills --}}
    {{> skills}}

    {{!-- Work experience --}}
    {{> experience}}

    {{!-- Key projects (optional) --}}
    {{#if options.showProjects}}
    {{> projects}}
    {{/if}}

    {{!-- Education --}}
    {{> education}}

  </main>
</body>
</html>
